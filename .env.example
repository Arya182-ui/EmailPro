# Environment Variables for Production Deployment

# ======================
# BACKEND (.env) - Render
# ======================

# Server Configuration
NODE_ENV=production
PORT=3000

# Database (Render PostgreSQL - Auto-generated)
DATABASE_URL=postgresql://user:password@host:port/database

# Redis (Render Redis - Auto-generated)  
REDIS_URL=redis://host:port

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here-change-this-in-production
JWT_EXPIRES_IN=24h

# Security
BCRYPT_ROUNDS=12

# Frontend URL (Vercel)
FRONTEND_URL=https://your-app-name.vercel.app

# Email Configuration (Add your SMTP details)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Security
BCRYPT_ROUNDS=12
ENCRYPTION_KEY=your-encryption-key-here-change-in-production

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_DIR=/tmp/uploads

# Campaign Configuration
OFFICE_HOURS_START=9
OFFICE_HOURS_END=17
MAX_BOUNCE_RATE=5

# Email Limits
DEFAULT_DAILY_LIMIT=100
MIN_DELAY_BETWEEN_EMAILS=15
MAX_DELAY_BETWEEN_EMAILS=20
BATCH_SIZE_MIN=10
BATCH_SIZE_MAX=15
BATCH_BREAK_DURATION=120

# SMTP Pool Configuration
SMTP_POOL_SIZE=5
SMTP_IDLE_TIMEOUT=300000
SMTP_MAX_CONNECTIONS=3
SMTP_MAX_MESSAGES=100
SMTP_RATE_LIMIT=5

# ======================
# FRONTEND (.env) - Vercel
# ======================

# API Configuration (Render Backend)
VITE_API_URL=https://your-backend-name.onrender.com

# App Configuration
VITE_APP_NAME=Email Automation Platform
VITE_APP_VERSION=1.0.0

# ======================
# DEPLOYMENT NOTES
# ======================

# 1. Backend: Deploy on Render with PostgreSQL + Redis
# 2. Frontend: Deploy on Vercel with Static Site Hosting
# 3. Generate JWT_SECRET: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 4. Use Gmail App Passwords for SMTP
# 5. Update URLs with actual deployment URLs
# 6. Both services auto-deploy on Git push